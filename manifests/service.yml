apiVersion: v1
kind: Service
metadata:
    name: "ascicdk8s-1a18"
    labels:
        app: "ascicdk8s-1a18"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ascicdk8s-1a18"